package dev.equo.ewt.support.variants;

import dev.equo.ewt.*;
import dev.equo.ewt.Stack;
import java.util.*;
import static dev.equo.ewt.EWT.*;

/** Generated by :generator:generator — do not edit by hand. */
public final class AnimatedRotationVariants {

  private AnimatedRotationVariants() {}

  public static final Map<String, List<PropExpectation>> EXPECTATIONS =
      Map.ofEntries(
          Map.entry("animatedRotation_allSet", List.of(PropExpectation.notNull("child"), PropExpectation.notNull("alignment"), PropExpectation.equalTo("filterQuality", FilterQuality.none), PropExpectation.notNull("curve"))),
          Map.entry("animatedRotation_boundary", List.of(PropExpectation.notNull("child"), PropExpectation.notNull("alignment"), PropExpectation.equalTo("filterQuality", FilterQuality.none), PropExpectation.notNull("curve")))
      );

  public static Widget animatedRotation_required() {
    return AnimatedRotation.animatedRotation().turns(1.0).duration(Duration.duration().milliseconds(200).build()).build();
  }

  public static Widget animatedRotation_allSet() {
    return AnimatedRotation.animatedRotation().turns(1.0).duration(Duration.duration().milliseconds(200).build()).child(SizedBox().width(1.0).height(1.0).build()).alignment(Alignment.center()).filterQuality(FilterQuality.none).curve(Curves.linear()).onEnd(() -> {}).build();
  }

  public static Widget animatedRotation_boundary() {
    return AnimatedRotation.animatedRotation().turns(1.0).duration(Duration.duration().milliseconds(200).build()).child(SizedBox().width(1.0).height(1.0).build()).alignment(Alignment.center()).filterQuality(FilterQuality.none).curve(Curves.linear()).onEnd(() -> {}).build();
  }

  public static Widget animatedRotation_callbackWired() {
    return AnimatedRotation.animatedRotation().turns(1.0).duration(Duration.duration().milliseconds(200).build()).onEnd(() -> {}).build();
  }

}
