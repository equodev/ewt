package dev.equo.ewt.support.variants;

import dev.equo.ewt.*;
import dev.equo.ewt.Stack;
import java.util.*;
import static dev.equo.ewt.EWT.*;

/** Generated by :generator:generator — do not edit by hand. */
public final class AnimatedSlideVariants {

  private AnimatedSlideVariants() {}

  public static final Map<String, List<PropExpectation>> EXPECTATIONS =
      Map.ofEntries(
          Map.entry("animatedSlide_allSet", List.of(PropExpectation.notNull("child"), PropExpectation.notNull("curve"))),
          Map.entry("animatedSlide_boundary", List.of(PropExpectation.notNull("child"), PropExpectation.notNull("curve")))
      );

  public static Widget animatedSlide_required() {
    return AnimatedSlide.animatedSlide().offset(Offset.zero()).duration(Duration.duration().milliseconds(200).build()).build();
  }

  public static Widget animatedSlide_allSet() {
    return AnimatedSlide.animatedSlide().offset(Offset.zero()).duration(Duration.duration().milliseconds(200).build()).child(SizedBox().width(1.0).height(1.0).build()).curve(Curves.linear()).onEnd(() -> {}).build();
  }

  public static Widget animatedSlide_boundary() {
    return AnimatedSlide.animatedSlide().offset(Offset.zero()).duration(Duration.duration().milliseconds(200).build()).child(SizedBox().width(1.0).height(1.0).build()).curve(Curves.linear()).onEnd(() -> {}).build();
  }

  public static Widget animatedSlide_callbackWired() {
    return AnimatedSlide.animatedSlide().offset(Offset.zero()).duration(Duration.duration().milliseconds(200).build()).onEnd(() -> {}).build();
  }

}
