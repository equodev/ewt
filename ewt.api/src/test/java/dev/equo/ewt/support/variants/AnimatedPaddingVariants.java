package dev.equo.ewt.support.variants;

import dev.equo.ewt.*;
import dev.equo.ewt.Stack;
import java.util.*;
import static dev.equo.ewt.EWT.*;

/** Generated by :generator:generator — do not edit by hand. */
public final class AnimatedPaddingVariants {

  private AnimatedPaddingVariants() {}

  public static final Map<String, List<PropExpectation>> EXPECTATIONS =
      Map.ofEntries(
          Map.entry("animatedPadding_allSet", List.of(PropExpectation.notNull("child"), PropExpectation.notNull("curve"))),
          Map.entry("animatedPadding_boundary", List.of(PropExpectation.notNull("child"), PropExpectation.notNull("curve")))
      );

  public static Widget animatedPadding_required() {
    return AnimatedPadding.animatedPadding(EdgeInsets_all(8.0).build()).duration(Duration.duration().milliseconds(200).build()).build();
  }

  public static Widget animatedPadding_allSet() {
    return AnimatedPadding.animatedPadding(EdgeInsets_all(8.0).build()).duration(Duration.duration().milliseconds(200).build()).child(SizedBox().width(1.0).height(1.0).build()).curve(Curves.linear()).onEnd(() -> {}).build();
  }

  public static Widget animatedPadding_boundary() {
    return AnimatedPadding.animatedPadding(EdgeInsets_all(8.0).build()).duration(Duration.duration().milliseconds(200).build()).child(SizedBox().width(1.0).height(1.0).build()).curve(Curves.linear()).onEnd(() -> {}).build();
  }

  public static Widget animatedPadding_callbackWired() {
    return AnimatedPadding.animatedPadding(EdgeInsets_all(8.0).build()).duration(Duration.duration().milliseconds(200).build()).onEnd(() -> {}).build();
  }

}
