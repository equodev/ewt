package dev.equo.ewt.support.variants;

import dev.equo.ewt.*;
import dev.equo.ewt.Stack;
import java.util.*;
import static dev.equo.ewt.EWT.*;

/** Generated by :generator:generator — do not edit by hand. */
public final class DropdownMenuItemVariants {

  private DropdownMenuItemVariants() {}

  public static final Map<String, List<PropExpectation>> EXPECTATIONS =
      Map.ofEntries(
          Map.entry("dropdownMenuItem_allSet", List.of(PropExpectation.equalTo("enabled", true), PropExpectation.notNull("alignment"))),
          Map.entry("dropdownMenuItem_boundary", List.of(PropExpectation.equalTo("enabled", false), PropExpectation.notNull("alignment")))
      );

  public static Widget dropdownMenuItem_required() {
    return DropdownMenuItem.dropdownMenuItem().child(SizedBox().width(1.0).height(1.0).build()).build();
  }

  public static Widget dropdownMenuItem_allSet() {
    return DropdownMenuItem.dropdownMenuItem().child(SizedBox().width(1.0).height(1.0).build()).onTap(() -> {}).enabled(true).alignment(Alignment.center()).build();
  }

  public static Widget dropdownMenuItem_boundary() {
    return DropdownMenuItem.dropdownMenuItem().child(SizedBox().width(1.0).height(1.0).build()).onTap(() -> {}).enabled(false).alignment(Alignment.center()).build();
  }

  public static Widget dropdownMenuItem_callbackWired() {
    return DropdownMenuItem.dropdownMenuItem().child(SizedBox().width(1.0).height(1.0).build()).onTap(() -> {}).build();
  }

}
