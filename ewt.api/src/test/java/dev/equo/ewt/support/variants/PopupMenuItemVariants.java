package dev.equo.ewt.support.variants;

import dev.equo.ewt.*;
import dev.equo.ewt.Stack;
import java.util.*;
import static dev.equo.ewt.EWT.*;

/** Generated by :generator:generator — do not edit by hand. */
public final class PopupMenuItemVariants {

  private PopupMenuItemVariants() {}

  public static Widget popupMenuItem_required() {
    return Material.material().child(PopupMenuItem.popupMenuItem().build()).build();
  }

  public static Widget popupMenuItem_allSet() {
    return Material.material().child(PopupMenuItem.popupMenuItem().onTap(() -> {}).enabled(true).height(1.0).padding(EdgeInsets_all(8.0).build()).textStyle(TextStyle.textStyle().build()).child(SizedBox().width(1.0).height(1.0).build()).build()).build();
  }

  public static Widget popupMenuItem_boundary() {
    return Material.material().child(PopupMenuItem.popupMenuItem().onTap(() -> {}).enabled(false).height(0.0).padding(EdgeInsets_all(8.0).build()).textStyle(TextStyle.textStyle().build()).child(SizedBox().width(1.0).height(1.0).build()).build()).build();
  }

  public static Widget popupMenuItem_callbackWired() {
    return Material.material().child(PopupMenuItem.popupMenuItem().onTap(() -> {}).build()).build();
  }

}
