package dev.equo.ewt.support.variants;

import dev.equo.ewt.*;
import dev.equo.ewt.Stack;
import java.util.*;
import static dev.equo.ewt.EWT.*;

/** Generated by :generator:generator — do not edit by hand. */
public final class DefaultTabControllerVariants {

  private DefaultTabControllerVariants() {}

  public static final Map<String, List<PropExpectation>> EXPECTATIONS =
      Map.ofEntries(
          Map.entry("defaultTabController_allSet", List.of(PropExpectation.equalTo("initialIndex", 1), PropExpectation.notNull("animationDuration"))),
          Map.entry("defaultTabController_boundary", List.of(PropExpectation.equalTo("initialIndex", 0), PropExpectation.notNull("animationDuration")))
      );

  public static Widget defaultTabController_required() {
    return DefaultTabController.defaultTabController(1).child(SizedBox().width(1.0).height(1.0).build()).build();
  }

  public static Widget defaultTabController_allSet() {
    return DefaultTabController.defaultTabController(1).child(SizedBox().width(1.0).height(1.0).build()).initialIndex(1).animationDuration(Duration.duration().milliseconds(200).build()).build();
  }

  public static Widget defaultTabController_boundary() {
    return DefaultTabController.defaultTabController(1).child(SizedBox().width(1.0).height(1.0).build()).initialIndex(0).animationDuration(Duration.duration().build()).build();
  }

}
