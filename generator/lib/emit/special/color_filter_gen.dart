part of '../../gen.dart';

/// Emitter for `ColorFilter` — same abstract-factory shape as ImageFilterGen.
class ColorFilterGen extends WidgetGen {
  ColorFilterGen(super.types, super.dartClass);

  @override
  bool get isAbstractFactoryHost => true;
}
