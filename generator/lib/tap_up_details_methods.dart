import 'package:flutter/gestures.dart';

class TapUpDetailsMethods {
  static Offset globalPosition(TapUpDetails self) => self.globalPosition;
  static Offset localPosition(TapUpDetails self) => self.localPosition;
}
